import 'package:flutter/material.dart';import 'package:get/get.dart';import '../../../../routes/app_route_name.dart';import '../../../../theme/app_color.dart';import '../../../../utils/base64_image_method.dart';import '../../../widget/photos_card.dart';import '../controller/home_screen_controller.dart';class UserDrawer extends StatelessWidget {  const UserDrawer({Key? key}) : super(key: key);  @override  Widget build(BuildContext context) {    return Drawer(      child: ListView(        children: [          GetBuilder<HomeScreenController>(            builder: (controller) {              return UserAccountsDrawerHeader(                onDetailsPressed: () {                  Get.toNamed(RouteName.updateProfileScreen);                },                currentAccountPictureSize: const Size(50, 50),                accountName: Text(                  controller.userProfileList[0].name,                  style: const TextStyle(                    color: Colors.white,                    fontSize: 18.0,                  ),                ),                accountEmail: Text(                  controller.userProfileList[0].email,                  style: TextStyle(                    color: Colors.white.withOpacity(0.7),                  ),                ),                currentAccountPicture: CircleAvatar(                  radius: 50,                  child: PhotosCard(                    img: decodeBase64Image(controller.userProfileList[0].photo),                  ),                ),                decoration: const BoxDecoration(color: AppColor.kPrimaryColor),              );            },          ),          ListTile(            leading: const Icon(Icons.help),            title: const Text('Help Center'),            onTap: () {              // Navigate to the Help Center screen              //  Get.toNamed(RouteName.helpCenterScreen);            },          ),          ListTile(            leading: const Icon(Icons.favorite),            title: const Text('Favorites'),            onTap: () {              // Navigate to the Favorites screen              //  Get.toNamed(RouteName.favoritesScreen);            },          ),          const Divider(),          ListTile(            leading: const Icon(Icons.history),            title: const Text('History'),            onTap: () {              // Navigate to the History screen              // Get.toNamed(RouteName.historyScreen);            },          ),          ListTile(            leading: const Icon(Icons.settings),            title: const Text('Settings'),            onTap: () {              // Navigate to the Settings screen              //  Get.toNamed(RouteName.settingsScreen);            },          ),          ListTile(            leading: const Icon(Icons.info),            title: const Text('About'),            onTap: () {              // Navigate to the About screen              // Get.toNamed(RouteName.aboutScreen);            },          ),          ListTile(            leading: const Icon(Icons.logout),            title: const Text('Log Out'),            onTap: () {              // Implement log out functionality              // Example: Sign the user out and redirect to the login screen              // Your implementation will depend on your authentication method              // Get.offAllNamed(RouteName.loginScreen);            },          ),        ],      ),    );  }}